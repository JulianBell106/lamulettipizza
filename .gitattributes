# Prevent firebase.js from being overwritten during branch merges.
# Each branch holds its own Firebase credentials — dev on develop, production on main.
# Git will always keep the current branch's version of this file during a merge.
js/firebase.js merge=ours
